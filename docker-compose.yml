services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: threshold
      POSTGRES_USER: threshold
      POSTGRES_PASSWORD: threshold
    ports:
      - "5432:5432"
    volumes:
      - threshold_pg:/var/lib/postgresql/data
volumes:
  threshold_pg:
